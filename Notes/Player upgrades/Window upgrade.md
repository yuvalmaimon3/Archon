create and pop up window(or box) for upgrade system
- the upgrade system size windows is a box with a 3 random player upgrade options. the player select only one upgrade for each box. and then it close.
- the upgrade box block any touch at the joystick. the upgrade box fill the whole screen and at the front so any thing behind the upgrade windows untouchable(find a good way to implement it.)
- each upgrade have an icon and shot description below.
- when play touch the upgrade icon the upgrade apply and make a click sound(for now there is no sound) and the upgrade windows close
- after an upgrade selected it apply on the player immediately
- when upgrade selected it cannot be an options any more i.e. it will not show again at the next upgrades windows any more. i.e. each upgrade can be selected only one time per run. (new run = all upgrades rest)
- work on appropriate branch
